{% extends 'base.html.twig' %}

{% block title %}Comm{% endblock %}

{% block body %}
    <h1>Comm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comm.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ comm.contenu }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ comm.createdAt ? comm.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comm_index') }}">back to list</a>

    <a href="{{ path('app_comm_edit', {'id': comm.id}) }}">edit</a>

    {{ include('comm/_delete_form.html.twig') }}
{% endblock %}
